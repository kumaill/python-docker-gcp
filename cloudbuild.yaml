steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - cloudFuncFlaskTest
  - --region=australia-southeast1
  - --source=.
  - --trigger-http
  - --runtime=python312
  - --entrypoint hello_world